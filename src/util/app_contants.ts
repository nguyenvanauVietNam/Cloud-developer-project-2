// Validator image url
export const validatior = {
    REGEX : /(?:https?):\/\/(\w+:?\w*)?(\S+)(:\d+)?(\/|\/([\w#!:.?+=&%!\-\/]))?/
}

// Message Response
export const messageResponse = {
    // image url is required
    IMAGE_URL_IS_REQUIRED : "image url is required",
     //image url is invalid
     IMAGE_URL_IS_INVALID : "image url is invalid"
}

// HTTP Status
export const httpStatus = {
    // SUCCESS: 200
    SUCCESS: 200,
    // BAD_REQUEST: 400
    BAD_REQUEST: 400
}